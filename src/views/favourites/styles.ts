import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: { 
    flex: 1,
    backgroundColor: "#fff",
    paddingBottom: 80,
 },
  emptyText: {
    marginTop: 40,
    textAlign: "center",
    fontSize: 16,
    color: "#888",
  },
  title: {
    fontSize: 32,
    fontWeight: 800,
    marginBottom: 8,
    paddingTop: 16,
    marginLeft: 16,
  }
});
