import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginBottom: 24,
  },

  title: {
    fontSize: 26,
    fontWeight: "800",
    marginBottom: 12,
  },
});
