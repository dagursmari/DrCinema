import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
    backgroundColor: "transparent",
    borderTopWidth: 0,
    borderBottomWidth: 0,
    paddingHorizontal: 0,
    marginHorizontal:10,
    paddingBottom:30,
  },

  inputContainer: {
    backgroundColor: "#eee",
    borderRadius: 10,
    height: 40,
  },
    });